"""Pytest configuration for the KFC chatbot project.

Placing this at the project root ensures the root is importable so tests
can ``import config``, ``import agent``, ``import api`` from anywhere.
"""